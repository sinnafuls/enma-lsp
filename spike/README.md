# spike/tree-sitter-enma

This directory contains the tree-sitter grammar spike from the M0.5 parser evaluation (see `docs/parser-decision.md` for the full decision record). It is retained as the v2.0 grammar candidate — not used at runtime, not packaged in the `.vsix`. Do not delete without updating `docs/parser-decision.md`.
